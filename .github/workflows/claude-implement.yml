name: Claude Auto Implementation

on:
  issue_comment:
    types: [created]

jobs:
  respond:
    name: Respond to Claude mention
    runs-on: ubuntu-latest
    if: |
      github.event.issue.pull_request == null &&
      contains(github.event.comment.body, '@claude')

    permissions:
      contents: read
      issues: write

    steps:
      - name: React to comment
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.reactions.createForIssueComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              comment_id: context.payload.comment.id,
              content: 'eyes'
            });

      - name: Comment on issue
        uses: actions/github-script@v7
        with:
          script: |
            const body = `👋 こんにちは！@claude とメンションいただきありがとうございます。

現在、このワークフローは基本的な応答のみを行います。

**自動実装は現在開発中です。**

手動で実装を進める場合は、このCLIセッションで直接指示してください。

---
Issue: #${context.issue.number}
Comment: ${context.payload.comment.html_url}`;

            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body: body
            });
